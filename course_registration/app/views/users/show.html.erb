<h4>View Your Courses</h4>
  <% @rosters.each do |roster| %>
<div>
<h3><%= roster.course.name %> - Section <%= roster.course.section %></h3>
    <a href="#">View Course Description</a><br>
    <%= roster.course.description %><br>
    <a href="#">View Students in This Course</a><br>
    <%= @student.first_name %>

  <% end %>
    <h4><p><%= link_to 'Register to Teach a New Course', new_course_path %></p></h4>
  <h3><%= link_to "Enroll a Student in a Course", courses_enroll_path %><br>
  <%= link_to "View All Students", students_path %></h3>
  </div>
