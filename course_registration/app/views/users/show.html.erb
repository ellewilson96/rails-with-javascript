<h3>View Your Courses</h3>
  <% @rosters.each do |roster| %>
<div>
<h3><%= roster.course.name %> - Section <%= roster.course.section %></h3>
    <h4><a href="#">View Course Description</a><br></h4>
    <%= roster.course.description %><br>
    <h4><a href="#">View Students in This Course</a><br></h4>
    <%= @student.first_name %></h4>

  <% end %>
    <h4><p><%= link_to 'Register to Teach a New Course', new_course_path %></p></h4>
  <h4><%= link_to "Enroll a Student in a Course", courses_enroll_path %><br>
  <%= link_to "View All Students", students_path %></h4>
  </div>
