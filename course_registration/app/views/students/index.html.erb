  <h3>All Flatiron High School Students</h3>
  <div class="student_info"><% @students.each do |student| %>
  <h4><%= student.first_name %> <%= student.last_name%></h4>
  <%= link_to "View #{student.first_name}'s Grades", student_grades_path(student) %><br>
  <%= link_to "Add New Grade for #{student.first_name}", new_student_grade_path(student) %><br>
<% end %></div><br>
<h4><%= link_to "Register a New Student", new_student_path %></h4>
  <h4><%= link_to "Enroll an Existing Student in a Course", rosters_enroll_path %></h4>
